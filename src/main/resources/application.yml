server:
  port: 8880
config:
  info: 3456789

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: wiki
    password: wiki
    url: jdbc:mysql://localhost:3306/db2019?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=UTC
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#打印SQL日志
logging:
  level:
    com.jiawa.wiki.mapper: trace
